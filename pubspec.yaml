name: testsolid
description: A test application for SOLID

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logger: ^0.6.0
  cupertino_icons: ^0.1.2
  provider: ^4.0.4
  shared_preferences: ^0.5.6+2


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true